# r2-connector — Titanium Host (clibuilder)

**Role:** connector | **Date:** 2026-08-09 | **Round:** R2  
**Inputs:** r1-critic (name/risk/pin), r1-executor (smoke green), r2-executor (Host + ladder + CI), live `index.html` / `scripts/smoke.sh` / README, kimi-k3 partial (timeout — composed from local evidence)

# State
- inf: R1 static hub landed; R2 executor already applied critic P0s — public brand **Titanium Host**, config as **risk ladder**, family needles + CI smoke. Dominant residual is **pin mechanics** (exact string still in hero/body) and **cross-repo brand lock** before Pages/inbound. [strong] — axes: install_clarity product_disambiguation stack_fidelity maintenance_cost evidence_quality deploy_latency
- risk: If smoke keeps grepping full triple-only while brew moves, evidence_quality goes green on a lie; if inbound sister sites still say "CLI Builder", rename is local-only and SEO collision returns; if risk ladder is demoted back into hero CTA, trust axis undoes R2.

# Dissent
- Executor may claim R2 "done" once HTML renames; connector keeps **version single-artifact** and **sister outbound copy** as open R2/R2.5 work — smoke green ≠ pin honesty.
- Planner may retain plan filename / WD `clibuilder`; connector accepts folder name, refuses public H1 "Builder".
- Capacity agents may add `version.json` fetch via Node; connector prefers static `assets/version.json` + bash sed or release checklist — no package.json.
- Distiller may re-lead with never/danger as "operator truth"; connector keeps ladder below fold, install CTA first.

# Rationale
Strange angle: R2 is not a greenfield plan — it is **critic→copy coupling**. The Pareto surface is the set of edits that improve naming + trust + pin honesty **without** raising deploy tax. CI smoke is the coupling rail: rename and ladder only stick if needles enforce "Titanium Host" + "risk ladder" and **stop** treating full semver as the sole truth gate. Cross-link R2 = naming + messaging + CI as one change-set, not three sequential milestones.

---

## Accepted Pareto-safe R2 moves (only)

Moves that improve ≥1 axis and harm none. Kill-list from critic stays closed.

### A. Naming (product_disambiguation · install_clarity)

1. **Public lock:** H1, `<title>`, meta description, nav brand, footer strong → **Titanium Host**; secondary phrase **Titanium CLI** / **codex-titanium** only. Never bare "Builder"; never "CLI Builder" as product.
2. **Repo/WD:** keep `clibuilder` until judge renames remote; one footer note that directory name ≠ product name (already R2 pattern — preserve).
3. **Smoke brand gate:** require `Titanium Host` in `index.html` **and** `README.md`; drop required string `Titanium CLI Builder` if any remain.
4. **CSS comment / file headers:** replace leftover "Titanium CLI Builder" in `style.css` (and smoke header comments) with Titanium Host — zero user-visible cost, kills agent re-copy of old brand.
5. **Disambiguation one-liner (keep, do not expand):** not Grok Builder · not Config Builder · not stock OpenAI codex. Related cards own deeper split.

### B. Risk ladder messaging (stack_fidelity · trust · install_clarity)

6. **Hero does not sell never/danger-full-access** as virtue bullets or status-pill payload. Hero = install hub + concurrency/SLIM host story.
7. **`#config` = risk ladder SSoT on this site:** Rung 0 read-only · Rung 1 workspace-write + on-request · Rung 2 Godspeed never + danger-full-access (isolated hosts). Keep exact policy keys for smoke fidelity.
8. **Preset install two-liner stays optional** under ladder, labeled "review before use / you own blast radius" — not hero primary CTA next to brew.
9. **Deep edit outbound only:** Config Builder link; no embedded TOML editor (maintenance_cost).
10. **Smoke:** require `risk ladder` + policy keys present; do **not** require them in hero-only regions (content presence, not marketing placement).

### C. Version / pin rot (evidence_quality · maintenance_cost)

11. **Family needles in smoke (keep R2 pattern):** `0.147` + `titanium.1` (or documented family), not sole hard fail on full `0.147.0-alpha.11+titanium.1` as the only version assertion.
12. **Single pin display site:** prefer one muted pin (status pill or `#what` code once). Avoid three independent prose copies of the full triple; README may say "see site pin / brew" rather than restate full triple.
13. **Optional R2.5 (still Pareto if tiny):** `assets/version.json` with `{ "pin": "…", "family": "0.147", "suffix": "titanium.1" }`; smoke greps HTML contains `pin` value from JSON. **No** Node toolchain to refresh — release checklist or one-line bash from codex-titanium tag.
14. **Verify copy:** "titanium family version" language (already R2) over "must equal exact triple forever."

### D. CI smoke + deploy evidence (evidence_quality · deploy_latency)

15. **Keep** `.github/workflows/smoke.yml` → `bash scripts/smoke.sh` on push/PR (R2 landed — do not delete).
16. **Smoke file gate set:** `index.html`, `style.css`, `README.md`, `scripts/smoke.sh`, `.github/workflows/smoke.yml`, `assets/` — fail closed if CI file vanishes.
17. **HTTP probe remains non-fatal** under python wall; content needles authoritative (matches R1 evidence).
18. **Outbound sister URLs in related (keep live hubs):** brew string · github.com/VeigaPunk/codex-titanium · veigapunk.github.io/xbgst-site · ds4cc.com + TITANIUM-HOST mention · xbrd-spark · grok-build-config-builder · homebrew-tap. Smoke may assert subset of URL needles for drift.
19. **Inbound reverse links deferred** until Pages URL exists and local smoke green on that URL — avoids sister-site rot (R1 connector #12 still holds).
20. **Still no** package.json / Nitro / Vercel / dual surface (critic K5/K8).

### E. Scope fence (maintenance_cost · stack_fidelity)

21. Site = thin funnel: install · verify · stack 64/16/64 · policy · related. No second TITANIUM-HOST matrix.
22. Never vendor codex-rs / formula / binaries here.
23. Secrets: install docs only; no op reveals.

### F. Explicit non-moves (do not ship under R2)

| Kill | Why |
| --- | --- |
| Reintroduce "Titanium CLI Builder" as H1 for plan continuity | product_disambiguation regression |
| Hero brag never + danger-full-access | trust / critic H4 |
| Triple-only smoke as sole version truth | pin rot |
| React/TanStack "parity" | maintenance_cost |
| Repo rename in this round without judge | deploy_latency / remote thrash |
| Restate full PATH matrix from TITANIUM-HOST | dual SSoT |

---

## Cross-link map (R2 change-set = one commit mental model)

| Track | Artifact | Needle / proof |
| --- | --- | --- |
| Naming | `index.html` brand + README H1 | `Titanium Host` |
| Messaging | `#config` ladder + footer | `risk ladder`, policy keys below fold |
| CI smoke | `scripts/smoke.sh` + workflow | family needles; workflow present |
| Pin | muted pill + family smoke | `0.147` / `titanium.1`; optional `assets/version.json` |
| Related | live hub URLs | xbgst Pages, ds4cc.com, binary SSoT |

Executor r2 already covers most of A/B/D on disk. Connector residual for judge/executor: **CSS brand comment**, **pin single-artifact (12–13)**, **inbound gate (19)**, **no capacity creep**.

# Axes check
| Axis | R2 effect |
| --- | --- |
| install_clarity | Brew remains primary CTA; Host naming reduces mis-click to Config/Grok Builder |
| product_disambiguation | Titanium Host lock + sibling cards |
| stack_fidelity | 64/16/64 + risk ladder truth without yolo hero |
| maintenance_cost | Static + bash CI; no framework; one pin source preferred |
| evidence_quality | Family needles + workflow; avoid frozen-lie full pin |
| deploy_latency | Pages-only path preserved |

# Obs
- kimi-k3: **BLOCKED** (session timeout after local explore/diff); composed from ≤ local Read/Grep of R1 + R2 executor + tree.
- Local: R2 executor status **done** (no commit); connector does not re-edit site — this file is handoff only.
- Pareto frontier walk: keep Host + ladder + CI; next cheap win is pin artifact + brand string hygiene, not new IA.
